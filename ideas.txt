halli has super mario world physics, pafu has hollow knight physics
fastfalling?
dashing?
attacking?
grapple hook / slingshotting? 

no dialogue >:(

overall theme: revisit (what a bad theme, right?)




halli and pafu get different music?

5 bosses
s
## Levels
recolour tilesets with different music for "variety" :)
three sizes: 12x4, 24x2, or 8x6
    screens should be grouped by scrollable regions of 2 rows or 2 columns to mimic performant scrolling on the NES.
what connects each area? 

act 1: 
    (steps are shown in-game as paw prints, ie "step 1" = 🐾-1
    step 1: forest floor
    
story development: grappling hook

act 2: 
    step 2: palace/ruins
        music: palace 
    
    step 3: canopy (forest/sky) (see: fluffy bluff galaxy)
    
    step 4: sky fortress
    
story development:
    - pafu/halli is evil?

act 3: pick order

    sand hell (desert/industrial)
        gimmick: platforms move
    copenhagen (grass/city)
        gimmick: non-linear, wraparound level, collectathon with quota (is that fun?)
        music: passion pit reeling
    organic beat (night mall/subway interior architecture)
        gimmick: grapple hook flips platforms (like in super mario galaxy 2)
        note: music sync gimmicks aren't practical for scratch


act 4: 
    step 8: lava + sky theme (night)
        music: nightmare castle


Collision shenanigans
    Currently this is colour-based, so all collision information can technically be put in one image file.
    However, the architecture is more complex than necessary for this because I don't know what could go wrong and we might need to switch to sprite-based collision. In the meantime, it should be ok to export a single collision layer as "solid".

## log

- abstract object backend has some hazy stuff, like local x/y scroll values

- need to program solids

- can functions take a behaviour as a parameter?

- consider structs for arbitrary actor-actor interactions

- object inheritance retains all scripts even if they are redefined (they coexist)

- use a list to track solids, but I need something else for actors because clones are more likely to be different from one another

- collision information ie wall detection slope detection? might have to come later 

- consider delta time - duplicate broadcasts in one frame are ignored :(

- v_max values might be a pain in the butt when dashing becomes a thing (decel_still is used for run > walk as well as unpredictably during almost all times)

- figure out how to scale jump force on x momentum

- huge castle rising out of the ground gimmick

- include sounds as part of animation data?

- todo: make pafu's hurtbox smaller when she does a dash step

- todo: add camera pan on look up/look down

- todo: add scrolling
    - make script to slice levels - done
    - solving for costume name from camera coordinates
    - position clones at camera coords
    - debug room coordinate view ?
    - pan function
    - player control camera
    
- scrolling in jump around simulator
    0. parameter setting (once)
    1. entities touch player
    2. player
        - read parameters to get scroll type.
            - set scroll speed and target based on position, velocity, and last grounded y-coordinate.
        - solve screen coordinates from scroll coordinates (should be a function, probably)
        
    3. background
        - spawns 2x2 grid of screens
        - each clone writes x/y positions and costume name to a list
        - caps scroll coordinates to scroll bounds
        - solve screen coordinates (again) for accurate screen transitions
        
    4. global
        - scroll tile positions
    5. entities

- todo: pafu
        - ledge hang/getup/jump
        - dash skid (proper)


- todo: make halli idle in time with the bpm of stage music

- todo: cooldown functionality (ie, move endlag)

- todo: control lock

- todo: test platform snap with ceilings

- get frame data for shaman crest (pafu's attacks)


- todo: simplify background layers to have only 1 collision layer

    
clones can't realistically impact the game until one frame after spawning, so if they have to be instant, consider a hitbox on the clone spawner.

pafu charge attack recoil = dash???

check direction behaviour re: projectile throw in other games

keypress variables are measured in frames, but other times are measured in real time (1 = 1/60 seconds)
    

    known bugs:
        pafu doesn't have a proper turnaround animation yet.
        
        pafu is sometimes considered grounded for 1 rendered frame if she clips a corner while jumping diagonally up.
        
        y value oscillates at the subpixel level if gravity is not high enough to move 1 pixel per frame - happens to pafu at 30hz and both characters at higher hz
        
        halli and pafu's jump heights aren't exactly the same, potentially due to rounding error
        
        game start is very laggy with compiler disabled; this is because of several "broadcast and wait" layers - it's practically lagless when they're all set to normal broadcasts, but I don't really want to do that.
        
        puff graphic doesn't show on screen, probably due to scroll displacement error


tips:
    - when changing a function/proc, make sure you check every existing instance of it.
    
    - try to minimise empty space around hitbox graphics if you can, since it seems to affect the collision properties.
    
    - sprites/objects don't natively play animation, even though you can queue animations. You have to add a proc to drive the animation counter.
    
    - clones run on the same frame they are spawned, but they won't respond to any broadcasts that happened that frame.
    
    - animations that have a static end frame generally still need to loop on that static frame.

"type mismatch: expected value, but got struct"
    - means that a variable was assigned from the struct list without that variable also being declared as the same type.


- not knowing exactly what the "x position" variable was really stung me today :(

- done: coyote time

- done: make delta time implementation for puff animation less horrible

- done: add smw-style running jump

- done: halli puff animation (basic)
- done: direction lock

- done: add soft platforms